apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: roundcube
  namespace: roundcube
spec:
  interval: 15m
  timeout: 5m
  driftDetection:
    mode: enabled
  chart:
    spec:
      chart: roundcube
      version: "0.8.3"
      sourceRef:
        kind: HelmRepository
        name: roundcube
  values: {}
