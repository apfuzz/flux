apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reloader
spec:
  interval: 15m
  values:
    reloader:
      deployment:
        resources:
          requests:
            memory: 64Mi
          limits:
            memory: 64Mi
