apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: reflector
spec:
  interval: 15m
  values:
    resources:
      requests:
        memory: 256Mi
      limits:
        memory: 512Mi
