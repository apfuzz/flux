---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault
  namespace: vault
spec:
  interval: 15m
  values:
    server:
      resources:
        requests:
          memory: 256Mi
        limits:
          memory: 256Mi
      ingress:
        annotations:
          external-dns.alpha.kubernetes.io/target: fivealive-ingress.gangsterkitties.com
        hosts:
          - host: vault.gangsterkitties.com
        tls:
          - hosts:
              - vault.gangsterkitties.com
