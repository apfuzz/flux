---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-operator
spec:
  interval: 15m
  values:
    web:
      networkPolicy:
        create: false
      ingress:
        annotations:
          external-dns.alpha.kubernetes.io/target: poptart-ingress.gangsterkitties.com
        hosts:
          - host: poptart-flux.gangsterkitties.com
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - poptart-flux.gangsterkitties.com
