---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openbao
  namespace: openbao
spec:
  interval: 15m
  values:
    server:
      gateway:
        httpRoute:
          annotations:
            external-dns.alpha.kubernetes.io/target: fivealive-gateway.gangsterkitties.com
          hosts:
            - openbao.gangsterkitties.com
