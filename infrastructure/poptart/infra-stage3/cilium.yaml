---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cilium
  namespace: cilium
spec:
  interval: 15m
  values:
    ingressController:
      service:
        annotations:
          metallb.io/loadBalancerIPs: 10.0.102.251
