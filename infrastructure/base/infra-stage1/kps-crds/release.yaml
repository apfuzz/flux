---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kps-crds
  namespace: monitoring
spec:
  interval: 15m
  timeout: 5m
  chartRef:
    kind: OCIRepository
    name: prometheus-operator-crds
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
