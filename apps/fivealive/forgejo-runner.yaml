---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitlab-runner
spec:
  interval: 15m
  values:
    dind:
      resources:
        requests:
          memory: 128Mi
        limits:
          memory: 256Mi
    resources:
      requests:
        memory: 32Mi
      limits:
        memory: 32Mi
