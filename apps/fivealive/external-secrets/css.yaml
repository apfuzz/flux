apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: https://vault.gangsterkitties.com
      path: gangsterkitties
      version: v2
      auth:
        kubernetes:
          mountPath: eso-fivealive
          role: kubernetes
          serviceAccountRef:
            name: vault
            namespace: vault
            audiences:
              - vault
          secretRef:
            name: eso-vault
            key: token
