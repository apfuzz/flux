---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: loki
  namespace: logging
spec:
  interval: 15m
  values:
    deploymentMode: SingleBinary
    loki:
      commonConfig:
        replication_factor: 1
      schemaConfig:
        configs:
          - from: "2024-04-01"
            object_store: s3
            store: tsdb
            schema: v13
            index:
              prefix: index_
              period: 24h
      storage:
        type: s3
        bucketNames:
          chunks: loki-chunks-87836769
          ruler: loki-ruler-87836769
        s3:
          endpoint: https://vhcompat.objectstorage.us-ashburn-1.oci.customer-oci.com
          region: us-ashburn-1
          accessKeyId: "${AWS_ACCESS_KEY_ID}"
          secretAccessKey: "${AWS_SECRET_ACCESS_KEY}"
    singleBinary:
      extraArgs:
        - "-config.expand-env=true"
      extraEnvFrom:
        - secretRef:
            name: loki-oci-credentials
      persistence:
        storageClass: longhorn
    write:
      replicas: 0
    read:
      replicas: 0
    backend:
      replicas: 0
    chunksCache:
      allocatedMemory: 1024
    monitoring:
      dashboards:
        # enabled: true
        enabled: false
      serviceMonitor:
        enabled: true
        labels:
          release: kps
