---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
spec:
  interval: 15m
  timeout: 5m
  chartRef:
    kind: OCIRepository
    name: podinfo
  values:
    replicas: 1
    # ingress:
    #   enabled: true
    #   annotations:
    #     external-dns.alpha.kubernetes.io/target: fivealive-ingress.gangsterkitties.com
    #   hosts:
    #     - host: podinfo-<name>.gangsterkitties.com
    #       paths:
    #         - path: /
    #           pathType: Prefix
    #   tls:
    #    - hosts:
    #        - podinfo-<name>.gangsterkitties.com
