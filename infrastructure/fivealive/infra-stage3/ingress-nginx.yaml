---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ingress-nginx
spec:
  interval: 15m
  values:
    controller:
      resources:
        requests:
          memory: 256Mi
        limits:
          memory: 256Mi
      service:
        annotations:
          external-dns.alpha.kubernetes.io/hostname: fivealive-ingress2.gangsterkitties.com
          lbipam.cilium.io/ips: 10.0.101.252
