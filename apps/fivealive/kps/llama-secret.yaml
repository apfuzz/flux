---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: llama
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: llama
  data:
    - secretKey: api-key
      remoteRef:
        key: llama
        property: api_key
